商业超体 V1.0：价值与定位引擎 (WPF 智能体) 升级版功能需求文档
一、 产品定位与核心交互升级
交互范式跃迁：从“填空问卷式”升级为“Copilot 伴随式诊断对话”。用户在左侧与 AI 顾问进行多轮深度对话，右侧的“商业画布”随着对话的深入，被 AI 自动、实时地精准填补和修正。

技术底座要求：在原有的 C# / WPF / MVVM 架构基础上，新增本地上下文记忆管理（Context Memory Management），确保国内大模型（如 DeepSeek/Kimi）的连续对话能力。

二、 核心 UI/UX 布局重构 (暗黑极客风双视口设计)
界面划分为左右两大核心视口（Split View），打破传统线性的“上一步/下一步”模式：

左半屏：AI 战略顾问对话舱 (Diagnostic Chat UI)

采用类似微信或 ChatGPT 的气泡对话框界面。

核心功能：系统主动发问，引导用户暴露出深层的商业底牌。支持多轮对话上下文滚动。

右半屏：动态商业画布监控区 (Dynamic Business Canvas)

采用卡片式仪表盘（Dashboard）布局。

核心功能：随着左侧对话的进行，右侧的四大核心卡片（护城河、客户痛点、情感溢价、商业蓝图）会自动亮起并填充提炼后的专业数据。

三、 核心业务模块（系统运行流水线）
模块一：多轮“老中医”把脉引擎 (Contextual Diagnostic Engine)

业务逻辑：内置预设的系统级 Prompt 提问链。

第一环：探寻独占资源。系统主动提问，挖掘用户是否掌握极具壁垒的物理资源（如特定产地的高端冷链冻干食材供应链），或独特的软性认知资产（如结合传统易经、八字哲学体系独创的决策推演模型）。

第二环：逼问隐性痛点。引导用户说出客户不购买该产品所面临的“隐形成本”与致命风险。

第三环：确认履约边界。诊断用户的时间精力和期望的利润结构。

技术实现 (ViewModel)：维护一个 ObservableCollection<Message> 用于绑定左侧对话 UI，并在本地 JSON 或轻量级 SQLite 中记录这批会话，作为后续生成的“全局语料库”。

模块二：实时信息提炼器 (Real-time Entity Extractor)

业务逻辑：不需要用户自己总结。AI 在后台默默分析用户的白话聊天记录，提取出“价值标签”。

技术实现：在左侧每完成两轮对话，后台静默发起一次非阻塞（Async）的 API 请求，要求大模型仅输出 JSON 格式的关键标签，并通过 MVVM 的双向绑定，瞬间更新到右半屏的“动态商业画布”对应位置。

模块三：降维对标与护城河构建 (Competitive Moat Builder)

业务逻辑：当诊断信息收集达到 80% 阈值（右侧画布大部分点亮）时，系统激活此功能。结合挖掘出的用户底牌与同行的盲区，生成错位竞争策略。

展示形式：在右侧弹出一个雷达图或对比表格，清晰展示“同行在卷什么” vs “我们独占什么”。

模块四：终极企划与超级签名自动熔铸 (Final Blueprint Caster)

业务逻辑：整合所有上下文记忆，一键生成带有强转化属性的商业蓝图。

输出内容：

一句话超级签名（我是谁，我用什么独家方法，帮谁解决什么问题）。

信任构建 SOP（展示哪些真实案例、背书资质）。

交付模式建议（高客单陪伴式 vs 轻量级自动化程序发放）。

技术实现：支持 Markdown 渲染，并提供导出为高质量 PDF 文档的功能，直接作为学员线下交付的“毕业作品”。

四、 容错与防呆机制 (Fail-Safe Mechanisms)
话题偏离纠正：若用户在左侧的回答严重跑题，AI 需具备“温柔拉回”的 Prompt 设定，确保诊断不偏离商业主线。

断点续传：自动保存对话进度（Session）。用户关闭软件后重新打开，系统应能提示“是否继续上一次的商业诊断？”。

ds：sk-c04a30e1e4fc40fcac715783c4a4160e
