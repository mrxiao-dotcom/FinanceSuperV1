<Window x:Class="商业超体价值与定位.Views.PromptsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:商业超体价值与定位.ViewModels"
        mc:Ignorable="d"
        Title="提示词配置"
        Height="720" Width="1080"
        MinHeight="600" MinWidth="900"
        WindowStartupLocation="CenterScreen"
        Background="#1A1A2E"
        d:DataContext="{d:DesignInstance Type=vm:PromptsViewModel}">

    <Window.Resources>
        <!-- 主操作按钮 -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#E94560"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#D63855"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#5A3A4A"/>
                                <Setter Property="Foreground" Value="#808080"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 次要按钮 -->
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#16213E"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#3A3F5C"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,7"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#252A40"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 左侧 ListBoxItem -->
        <Style TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#C0C0C0"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="#2A3040"
                                BorderThickness="1"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#2A3A5C"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="BorderBrush" Value="#E94560"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1F2535"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 顶部标题栏 -->
        <Border Grid.Row="0" Background="#16213E" Padding="20,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="⚙️ 提示词配置"
                               Foreground="White"
                               FontSize="20"
                               FontWeight="Bold"/>
                    <TextBlock Text="配置诊断对话、4 周大纲、日常任务等所有 LLM 提示词。修改后即时生效。"
                               Foreground="#808080"
                               FontSize="12"
                               Margin="0,4,0,0"/>
                </StackPanel>

                <Button Grid.Column="1"
                        Content="🔄 全部恢复默认"
                        Style="{StaticResource SecondaryButton}"
                        Command="{Binding RestoreAllCommand}"
                        VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- 主内容区：左侧 prompt 列表 + 右侧编辑器 -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 左侧 prompt 列表 -->
            <Border Grid.Column="0" Background="#16213E" Padding="12">
                <StackPanel>
                    <TextBlock Text="提示词列表（共 7 项）"
                               Foreground="#808080"
                               FontSize="11"
                               FontWeight="SemiBold"
                               Margin="8,0,0,10"/>

                    <ListBox ItemsSource="{Binding PromptList}"
                             SelectedItem="{Binding SelectedPrompt}"
                             Background="Transparent"
                             BorderThickness="0"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock Text="{Binding DisplayName}"
                                               Foreground="White"
                                               FontSize="12"
                                               FontWeight="SemiBold"
                                               TextWrapping="Wrap"/>
                                    <TextBlock Text="{Binding Key}"
                                               Foreground="#606060"
                                               FontSize="10"
                                               FontFamily="Consolas, monospace"
                                               Margin="0,2,0,0"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>
            </Border>

            <!-- 分隔条 -->
            <GridSplitter Grid.Column="1"
                          Width="3"
                          Background="#16213E"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Stretch"/>

            <!-- 右侧编辑器 -->
            <Grid Grid.Column="2" Margin="16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- 当前 prompt 的描述与占位符 -->
                <Border Grid.Row="0"
                        Background="#252A40"
                        CornerRadius="6"
                        Padding="16,12"
                        Margin="0,0,0,12">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📝"
                                       FontSize="14"
                                       Margin="0,0,8,0"
                                       VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding CurrentPromptDisplayName}"
                                       Foreground="White"
                                       FontSize="16"
                                       FontWeight="Bold"
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                        <TextBlock Text="{Binding CurrentPromptDescription}"
                                   Foreground="#D0D0D0"
                                   FontSize="12"
                                   TextWrapping="Wrap"
                                   Margin="0,8,0,0"
                                   LineHeight="18"/>
                        <Border Background="#1F2535"
                                CornerRadius="4"
                                Padding="8,6"
                                Margin="0,10,0,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="占位符："
                                           Foreground="#FF9800"
                                           FontSize="11"
                                           FontWeight="SemiBold"
                                           VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding CurrentPromptPlaceholders}"
                                           Foreground="#F0F0F0"
                                           FontSize="11"
                                           FontFamily="Consolas, monospace"
                                           VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- 操作按钮区 -->
                <StackPanel Grid.Row="1"
                            Orientation="Horizontal"
                            Margin="0,0,0,8">
                    <Button Content="🔄 恢复此项默认"
                            Style="{StaticResource SecondaryButton}"
                            Command="{Binding RestoreCurrentCommand}"
                            Margin="0,0,8,0"/>
                    <TextBlock Text="{Binding StatusMessage}"
                               Foreground="#808080"
                               FontSize="11"
                               VerticalAlignment="Center"
                               Margin="20,0,0,0"/>
                </StackPanel>

                <!-- 大文本框 -->
                <Border Grid.Row="2"
                        Background="#252A40"
                        CornerRadius="6"
                        Padding="2">
                    <TextBox x:Name="PromptEditor"
                             Text="{Binding CurrentPromptText, UpdateSourceTrigger=PropertyChanged}"
                             Background="Transparent"
                            Foreground="#D0D0D0"
                            BorderThickness="0"
                            Padding="12,10"
                            FontFamily="Consolas, Microsoft YaHei UI, monospace"
                            FontSize="12"
                            AcceptsReturn="True"
                            AcceptsTab="True"
                            TextWrapping="Wrap"
                            VerticalScrollBarVisibility="Auto"
                            HorizontalScrollBarVisibility="Auto"
                            SpellCheck.IsEnabled="False"/>
                </Border>
            </Grid>
        </Grid>

        <!-- 底部按钮区 -->
        <Border Grid.Row="2" Background="#16213E" Padding="20,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="⚠️ 提示：修改提示词后立即生效，可能影响 LLM 输出风格。建议先备份。"
                           Foreground="#808080"
                           FontSize="11"
                           VerticalAlignment="Center"/>

                <Button Grid.Column="1"
                        Content="{Binding SaveButtonText}"
                        Style="{StaticResource PrimaryButton}"
                        Command="{Binding SaveCommand}"
                        Padding="20,8"/>
            </Grid>
        </Border>
    </Grid>
</Window>