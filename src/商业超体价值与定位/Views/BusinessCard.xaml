<UserControl x:Class="商业超体价值与定位.Views.BusinessCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:models="clr-namespace:商业超体价值与定位.Models"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=models:MoatCard}"
             d:DesignDataContext="{d:DesignInstance Type=models:MoatCard}">

    <UserControl.Resources>
        <Style x:Key="ColorToBrush" TargetType="Border">
            <Setter Property="Background" Value="#3A3F5C"/>
        </Style>
    </UserControl.Resources>

    <Border CornerRadius="12"
            Padding="20"
            BorderThickness="2">
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="Background" Value="#252A40"/>
                <Setter Property="BorderBrush" Value="#3A3F5C"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsActivated}" Value="True">
                        <Setter Property="BorderBrush" Value="{Binding Color}"/>
                        <Setter Property="Effect">
                            <Setter.Value>
                                <DropShadowEffect Color="{Binding Color}"
                                                  BlurRadius="20"
                                                  ShadowDepth="0"
                                                  Opacity="0.5"/>
                            </Setter.Value>
                        </Setter>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 标题区域 -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="{Binding Title}"
                               FontSize="18"
                               FontWeight="Bold"
                               Foreground="White"/>
                    <TextBlock Text="{Binding Description}"
                               FontSize="12"
                               Foreground="#A0A0A0"
                               Margin="0,2,0,0"/>
                </StackPanel>

                <!-- 激活状态指示器 -->
                <Border Grid.Column="1"
                        CornerRadius="10"
                        Padding="8,4"
                        VerticalAlignment="Center">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Background" Value="#3A3F5C"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsActivated}" Value="True">
                                    <Setter Property="Background" Value="#4CAF50"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <TextBlock FontSize="11" Foreground="White">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Text" Value="等待中..."/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsActivated}" Value="True">
                                        <Setter Property="Text" Value="✓ 已激活"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Border>
            </Grid>

            <!-- 内容区域 -->
            <TextBlock Grid.Row="1"
                       Text="{Binding Content}"
                       Foreground="#D0D0D0"
                       TextWrapping="Wrap"
                       Margin="0,15,0,10"
                       FontSize="14"
                       LineHeight="22"/>

            <!-- 标签列表 -->
            <ItemsControl Grid.Row="2"
                          ItemsSource="{Binding ExclusiveResources}"
                          Visibility="{Binding IsActivated, Converter={StaticResource BoolToVisConverter}}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border CornerRadius="4"
                                Padding="8,4"
                                Margin="0,0,5,5">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="{Binding Color}"/>
                                </Style>
                            </Border.Style>
                            <TextBlock Text="{Binding}"
                                       FontSize="12"
                                       Foreground="White"/>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </Border>
</UserControl>
